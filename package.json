{
  "name": "caxumba-video-saver",
  "version": "0.1.0",
  "private": true,
  "description": "Extensao de navegador que detecta e baixa videos MP4/HLS servidos diretamente por uma pagina.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "node scripts/build.mjs --clean-only",
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.23.1",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.12.10",
    "@ffmpeg/ffmpeg": "^0.12.10"
  }
}
